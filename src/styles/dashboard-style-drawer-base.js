// Shared spacing for drawer-only content that sits outside the timeline owner.
export function dashboardStyleDrawerBase() {
  return `.stopover-break.first { border-top: 0; margin-top: 0; }
.drawer-panel .pain-grid { padding: 0 26px; }
.drawer-panel .assumption-list,
.drawer-panel .drawer-advice { margin: 14px 26px; }`;
}
